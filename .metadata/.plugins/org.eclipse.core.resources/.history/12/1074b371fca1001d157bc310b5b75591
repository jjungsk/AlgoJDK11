package study.ssafy.bundler;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.ArrayDeque;
import java.util.Deque;
import java.util.StringTokenizer;

public class 컨베이어벨트위의로봇_정세권 {
	
	static int N, K, duCunt;
	static Deque<Integer> belt, robot;
	
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringTokenizer st;
		
		st = new StringTokenizer(br.readLine());
		N = Integer.parseInt(st.nextToken());
		K = Integer.parseInt(st.nextToken());
		
		belt = new ArrayDeque<Integer>();
		robot = new ArrayDeque<Integer>();
		
		st = new StringTokenizer(br.readLine());
		for (int i = 0; i < N*2; i++)
			belt.addLast(Integer.parseInt(st.nextToken()));
		
		System.out.println(belt);
		
		
	}
	
	private static void moveBelt() {
		
		
	}

}
